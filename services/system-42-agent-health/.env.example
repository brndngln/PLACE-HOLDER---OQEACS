# ──────────────────────────────────────────────
# System 42 — Agent Health Monitor
# ──────────────────────────────────────────────

# Service identity
SERVICE_NAME=agent-health
SERVICE_PORT=9635
LOG_LEVEL=INFO

# PostgreSQL — stores agent performance history, test results, benchmarks
DATABASE_URL=postgresql+asyncpg://omni:omni@omni-postgres:5432/omni_agent_health

# Redis — caching, scheduler lock, rate-limiting
REDIS_URL=redis://omni-redis:6379/8

# LiteLLM proxy — used to send prompts to agents under test
LITELLM_URL=http://omni-litellm:4000

# Mattermost — alert channel for critical failures
MATTERMOST_WEBHOOK_URL=http://omni-mattermost:8065/hooks/agent-health

# Sandbox service — optional code execution environment
SANDBOX_URL=http://omni-sandbox:9620
