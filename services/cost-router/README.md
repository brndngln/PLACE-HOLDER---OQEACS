# Cost Aware Task Router

System 8/14 of the Omni Quantum Elite Generation Intelligence Layer.

## Service
- Name: `omni-cost-router`
- Port: `9632`
- Critical: `false`

## Run
```bash
docker compose -f docker-compose.yml up -d --build
```

## Endpoints
- `GET /health`
- `GET /ready`
- `GET /metrics`
- `GET /info`
- `GET /docs`
