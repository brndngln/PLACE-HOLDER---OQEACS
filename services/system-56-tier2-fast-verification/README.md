# System 56 - Tier2 Fast Verification

Service codename: tier2-fast-verification

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-56-tier2-fast-verification/docker-compose.yml up -d --build

Default port:
10056
