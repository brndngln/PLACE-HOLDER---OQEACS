# Codebase Comprehension Engine

System 2/14 of the Omni Quantum Elite Generation Intelligence Layer.

## Service
- Name: `omni-comprehension-engine`
- Port: `9621`
- Critical: `true`

## Run
```bash
docker compose -f docker-compose.yml up -d --build
```

## Endpoints
- `GET /health`
- `GET /ready`
- `GET /metrics`
- `GET /info`
- `GET /docs`
