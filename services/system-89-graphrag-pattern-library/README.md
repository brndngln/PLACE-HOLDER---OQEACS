# System 89 - Graphrag Pattern Library

Service codename: graphrag-pattern-library

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-89-graphrag-pattern-library/docker-compose.yml up -d --build

Default port:
10089
