# System 131 - Feature Flags Unleash

Service codename: feature-flags-unleash

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-131-feature-flags-unleash/docker-compose.yml up -d --build

Default port:
10131
