# System 59 - Semantic Intent Verification

Service codename: semantic-intent-verification

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-59-semantic-intent-verification/docker-compose.yml up -d --build

Default port:
10059
