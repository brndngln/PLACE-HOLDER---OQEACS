# System 165 - Diagram Generation

Service codename: diagram-generation

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-165-diagram-generation/docker-compose.yml up -d --build

Default port:
10165
