# System 73 - Cross Project Learning

Service codename: cross-project-learning

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-73-cross-project-learning/docker-compose.yml up -d --build

Default port:
10073
