# System 63 - Adr Templates Tracking

Service codename: adr-templates-tracking

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-63-adr-templates-tracking/docker-compose.yml up -d --build

Default port:
10063
