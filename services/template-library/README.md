# Project Template Library

System 5/14 of the Omni Quantum Elite Generation Intelligence Layer.

## Service
- Name: `omni-template-library`
- Port: `9627`
- Critical: `false`

## Run
```bash
docker compose -f docker-compose.yml up -d --build
```

## Endpoints
- `GET /health`
- `GET /ready`
- `GET /metrics`
- `GET /info`
- `GET /docs`
