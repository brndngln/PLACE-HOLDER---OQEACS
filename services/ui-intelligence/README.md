# UI UX Generation Intelligence

System 9/14 of the Omni Quantum Elite Generation Intelligence Layer.

## Service
- Name: `omni-ui-intelligence`
- Port: `9625`
- Critical: `false`

## Run
```bash
docker compose -f docker-compose.yml up -d --build
```

## Endpoints
- `GET /health`
- `GET /ready`
- `GET /metrics`
- `GET /info`
- `GET /docs`
