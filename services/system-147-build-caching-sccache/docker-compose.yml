version: '3.9'

networks:
  omni-quantum-network:
    external: true

services:
  system-147-build-caching-sccache:
    build:
      context: .
      dockerfile: Dockerfile
    image: omni-system-147-build-caching-sccache:1.0.0
    container_name: omni-system-147-build-caching-sccache
    env_file:
      - ../../.env
    environment:
      SERVICE_NAME: system-147-build-caching-sccache
      SERVICE_ID: '147'
      SERVICE_PORT: '10147'
      LOG_LEVEL: ${LOG_LEVEL:-info}
      LOG_FORMAT: json
    ports:
      - '10147:10147'
    networks:
      - omni-quantum-network
    healthcheck:
      test: ['CMD', 'python', '-c', "import urllib.request; urllib.request.urlopen('http://localhost:10147/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 128M
          cpus: '0.25'
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:size=128M
    labels:
      omni.quantum.component: system-147-build-caching-sccache
      omni.quantum.tier: wave-backlog
      omni.quantum.critical: 'false'
      omni.quantum.system: '147'
