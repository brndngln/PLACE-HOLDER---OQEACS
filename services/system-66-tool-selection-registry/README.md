# System 66 - Tool Selection Registry

Service codename: tool-selection-registry

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-66-tool-selection-registry/docker-compose.yml up -d --build

Default port:
10066
