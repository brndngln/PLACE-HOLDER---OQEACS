version: '3.9'

networks:
  omni-quantum-network:
    external: true

services:
  distributed-tracing-jaeger:
    build:
      context: .
      dockerfile: Dockerfile
    image: omni-distributed-tracing-jaeger:1.0.0
    container_name: omni-distributed-tracing-jaeger
    env_file:
      - ../../.env
    environment:
      SERVICE_NAME: distributed-tracing-jaeger
      SERVICE_ID: '64'
      SERVICE_PORT: '16686'
      LOG_LEVEL: \info
      LOG_FORMAT: json
    ports:
      - '16686:16686'
    networks:
      - omni-quantum-network
    healthcheck:
      test: ['CMD', 'python', '-c', "import urllib.request; urllib.request.urlopen('http://localhost:16686/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 128M
          cpus: '0.25'
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:size=128M
    labels:
      omni.quantum.component: distributed-tracing-jaeger
      omni.quantum.tier: gap-remediation
      omni.quantum.critical: 'false'
      omni.quantum.system: '64'
    logging:
      driver: json-file
      options:
        max-size: '50m'
        max-file: '5'
