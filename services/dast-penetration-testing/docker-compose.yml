version: '3.9'

networks:
  omni-quantum-network:
    external: true

services:
  dast-penetration-testing:
    build:
      context: .
      dockerfile: Dockerfile
    image: omni-dast-penetration-testing:1.0.0
    container_name: omni-dast-penetration-testing
    env_file:
      - ../../.env
    environment:
      SERVICE_NAME: dast-penetration-testing
      SERVICE_ID: '67'
      SERVICE_PORT: '8090'
      LOG_LEVEL: \info
      LOG_FORMAT: json
    ports:
      - '8090:8090'
    networks:
      - omni-quantum-network
    healthcheck:
      test: ['CMD', 'python', '-c', "import urllib.request; urllib.request.urlopen('http://localhost:8090/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 128M
          cpus: '0.25'
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:size=128M
    labels:
      omni.quantum.component: dast-penetration-testing
      omni.quantum.tier: gap-remediation
      omni.quantum.critical: 'false'
      omni.quantum.system: '67'
    logging:
      driver: json-file
      options:
        max-size: '50m'
        max-file: '5'
