# System 139 - Translation Management Tolgee

Service codename: translation-management-tolgee

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-139-translation-management-tolgee/docker-compose.yml up -d --build

Default port:
10139
