# System 68 - Data Validation Framework

Service codename: data-validation-framework

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /validate-data
- GET /metrics

Run:

docker compose -f services/data-validation-framework/docker-compose.yml up -d --build

Default port:
9668
