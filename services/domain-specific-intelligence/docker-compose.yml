version: '3.9'

networks:
  omni-quantum-network:
    external: true

services:
  domain-specific-intelligence:
    build:
      context: .
      dockerfile: Dockerfile
    image: omni-domain-specific-intelligence:1.0.0
    container_name: omni-domain-specific-intelligence
    env_file:
      - ../../.env
    environment:
      SERVICE_NAME: domain-specific-intelligence
      SERVICE_ID: '59'
      SERVICE_PORT: '9659'
      LOG_LEVEL: \info
      LOG_FORMAT: json
    ports:
      - '9659:9659'
    networks:
      - omni-quantum-network
    healthcheck:
      test: ['CMD', 'python', '-c', "import urllib.request; urllib.request.urlopen('http://localhost:9659/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 128M
          cpus: '0.25'
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:size=128M
    labels:
      omni.quantum.component: domain-specific-intelligence
      omni.quantum.tier: gap-remediation
      omni.quantum.critical: 'false'
      omni.quantum.system: '59'
    logging:
      driver: json-file
      options:
        max-size: '50m'
        max-file: '5'
