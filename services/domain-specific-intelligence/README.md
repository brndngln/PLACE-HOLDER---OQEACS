# System 59 - Domain Specific Intelligence

Service codename: domain-specific-intelligence

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /domains
- GET /metrics

Run:

docker compose -f services/domain-specific-intelligence/docker-compose.yml up -d --build

Default port:
9659
