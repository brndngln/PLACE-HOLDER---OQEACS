version: '3.9'

networks:
  omni-quantum-network:
    external: true

services:
  continuous-profiling-pyroscope:
    build:
      context: .
      dockerfile: Dockerfile
    image: omni-continuous-profiling-pyroscope:1.0.0
    container_name: omni-continuous-profiling-pyroscope
    env_file:
      - ../../.env
    environment:
      SERVICE_NAME: continuous-profiling-pyroscope
      SERVICE_ID: '51'
      SERVICE_PORT: '4040'
      LOG_LEVEL: \info
      LOG_FORMAT: json
    ports:
      - '4040:4040'
    networks:
      - omni-quantum-network
    healthcheck:
      test: ['CMD', 'python', '-c', "import urllib.request; urllib.request.urlopen('http://localhost:4040/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 128M
          cpus: '0.25'
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:size=128M
    labels:
      omni.quantum.component: continuous-profiling-pyroscope
      omni.quantum.tier: gap-remediation
      omni.quantum.critical: 'false'
      omni.quantum.system: '51'
    logging:
      driver: json-file
      options:
        max-size: '50m'
        max-file: '5'
