# System 57 - Tier3 Deep Verification

Service codename: tier3-deep-verification

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-57-tier3-deep-verification/docker-compose.yml up -d --build

Default port:
10057
