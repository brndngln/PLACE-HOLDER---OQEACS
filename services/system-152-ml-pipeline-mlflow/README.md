# System 152 - Ml Pipeline Mlflow

Service codename: ml-pipeline-mlflow

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-152-ml-pipeline-mlflow/docker-compose.yml up -d --build

Default port:
10152
