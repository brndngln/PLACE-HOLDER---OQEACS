version: "3.9"

services:
  failure_corpus_ingester:
    build:
      context: .
      dockerfile: Dockerfile
    image: omni/failure-corpus-ingester:1.0.0
    container_name: omni-failure-corpus-ingester
    restart: unless-stopped
    env_file:
      - ../../.env
    environment:
      SYSTEM_ID: "92"
      SYSTEM_NAME: "failure-corpus-ingester"
      SYSTEM_PORT: "9764"
      LOG_LEVEL: \INFO
    ports:
      - "9764:9764"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:9764/health')\" || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    labels:
      omni.quantum.component: failure-corpus-ingester
      omni.quantum.tier: knowledge
      omni.quantum.critical: "false"
    networks:
      - omni-quantum-network

networks:
  omni-quantum-network:
    external: true
