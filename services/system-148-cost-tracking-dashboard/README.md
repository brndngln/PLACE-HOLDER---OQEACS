# System 148 - Cost Tracking Dashboard

Service codename: cost-tracking-dashboard

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-148-cost-tracking-dashboard/docker-compose.yml up -d --build

Default port:
10148
