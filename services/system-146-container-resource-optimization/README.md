# System 146 - Container Resource Optimization

Service codename: container-resource-optimization

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-146-container-resource-optimization/docker-compose.yml up -d --build

Default port:
10146
