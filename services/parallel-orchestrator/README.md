# Parallel Task Orchestrator

System 7/14 of the Omni Quantum Elite Generation Intelligence Layer.

## Service
- Name: `omni-parallel-orchestrator`
- Port: `9623`
- Critical: `false`

## Run
```bash
docker compose -f docker-compose.yml up -d --build
```

## Endpoints
- `GET /health`
- `GET /ready`
- `GET /metrics`
- `GET /info`
- `GET /docs`
