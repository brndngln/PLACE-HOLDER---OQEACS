version: '3.9'

networks:
  omni-quantum-network:
    external: true

services:
  system-67-mcp-package-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: omni-system-67-mcp-package-server:1.0.0
    container_name: omni-system-67-mcp-package-server
    env_file:
      - ../../.env
    environment:
      SERVICE_NAME: system-67-mcp-package-server
      SERVICE_ID: '67'
      SERVICE_PORT: '10067'
      LOG_LEVEL: ${LOG_LEVEL:-info}
      LOG_FORMAT: json
    ports:
      - '10067:10067'
    networks:
      - omni-quantum-network
    healthcheck:
      test: ['CMD', 'python', '-c', "import urllib.request; urllib.request.urlopen('http://localhost:10067/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 128M
          cpus: '0.25'
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:size=128M
    labels:
      omni.quantum.component: system-67-mcp-package-server
      omni.quantum.tier: wave-backlog
      omni.quantum.critical: 'false'
      omni.quantum.system: '67'
