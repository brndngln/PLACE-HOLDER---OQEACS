# System 74 - Spec Driven Development

Service codename: spec-driven-development

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-74-spec-driven-development/docker-compose.yml up -d --build

Default port:
10074
