# System 149 - Prompt Cache Optimization

Service codename: prompt-cache-optimization

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-149-prompt-cache-optimization/docker-compose.yml up -d --build

Default port:
10149
