# Hallucination Detection Layer

System 3/14 of the Omni Quantum Elite Generation Intelligence Layer.

## Service
- Name: `omni-hallucination-detector`
- Port: `9622`
- Critical: `true`

## Run
```bash
docker compose -f docker-compose.yml up -d --build
```

## Endpoints
- `GET /health`
- `GET /ready`
- `GET /metrics`
- `GET /info`
- `GET /docs`
