# System 71 - Anti Pattern Knowledge Base

Service codename: anti-pattern-knowledge-base

Endpoints:
- GET /health
- GET /ready
- GET /info
- GET /metrics

Run:
docker compose -f services/system-71-anti-pattern-knowledge-base/docker-compose.yml up -d --build

Default port:
10071
